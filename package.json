{
  "name": "e2b-agents-runtime-tools",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "template:build": "npm run build && node runtime/openclaw-template/build.mjs"
  },
  "dependencies": {
    "e2b": "^2.10.5"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}
